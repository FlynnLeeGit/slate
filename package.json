{
  "name": "just-build",
  "scripts": {
    "dev": "docker-compose up",
    "build": "docker run --rm -v $PWD:/usr/src/app/source -w /usr/src/app/source slate_app bundle exec middleman build --clean"
  }
}